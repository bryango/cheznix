#!/bin/bash
# get the position of a package

FILE=$HOME/@pkgs-position@
CODE_VIEWER=@viewer@

package=$1
shift

pos=$(
    package=$package nix eval --impure --file "$FILE" \
        --raw --apply toString \
    | xargs
)

# shellcheck disable=2086
$CODE_VIEWER $pos
